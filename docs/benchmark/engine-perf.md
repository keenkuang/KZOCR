# 引擎性能基线对比结论（PaddleOCR vs RapidOCR）

> 归档日期：2026-07-18
> 数据源：`scripts/bench_engine_compare.py`（DPI 对比）+ `scripts/bench_paddle_perf.py`（多页连续）+ `scripts/bench_char_boxes.py`（char_boxes 开销对照）
> 关联提交：`8cf0289`（PaddleOCR 进程级单例 + CPU 推理优化）、`5f2a4d8`、`53b4f84`、`d99e3fb`（char_boxes 开销对照基准）

## 1. 测试环境

- 引擎：PaddleOCR（`paddleocr` 已装）、RapidOCR（`rapidocr_onnxruntime` 已装）
- 样本：`/home/keen/Documents/test_10_pages.pdf`（10 页古籍扫描，约 0.5MB）
- 硬件：CPU 推理（无 GPU 加速），单进程
- 指标：`run_page` 端到端耗时（含 PDF 渲染 `get_pixmap` + 引擎推理），不含模型首次下载

## 2. 引擎 × DPI 对比（每引擎 5 页）

| DPI | 引擎 | 首屏(s) | 单页均值(s) | 识别字数 | 5 页总耗时(s) |
|----:|------|--------:|------------:|---------:|--------------:|
| 72  | PaddleOCR | 25.0 | 21.9 | 986 | 112.7 |
| 72  | RapidOCR  |  2.1 |  1.7 | 845 |   8.8 |
| 150 | PaddleOCR | 40.6 | 28.0 | 979 | 152.5 |
| 150 | RapidOCR  |  2.6 |  1.9 | 850 |  10.1 |

## 3. PaddleOCR 多页连续稳定性（DPI=150，单实例 10 页）

| 页 | 字数 | 耗时(s) | 备注 |
|----:|-----:|--------:|------|
| 0  |  979 | 35.4 | 含模型加载（进程单例首次实例化） |
| 1  |  823 | 26.0 | |
| 2  |  735 | 23.7 | |
| 3  |  823 | 26.6 | |
| 4  | 1034 | 34.2 | |
| 5  |  834 | 27.4 | |
| 6  |  820 | 26.7 | |
| 7  |  916 | 28.8 | |
| 8  |  843 | 29.3 | |
| 9  |  678 | 24.0 | |

- 首次加载 + 首页：**35.4s**
- 后续 9 页均值：**27.4s/页**（区间 23.7–34.2s）
- 10 页总耗时：**282.2s**

逐页耗时波动 < 2×，无随页数递增的退化趋势 → **`8cf0289` 进程级单例生效**，模型仅在首页实例化一次，后续页稳定复用。

## 4. 结论

### 4.1 DPI 影响（图像分辨率 → 推理耗时）
- PaddleOCR：DPI 150 比 72 慢约 **1.28×**（28.0s vs 21.9s/页）。
- RapidOCR：DPI 150 比 72 慢约 **1.12×**（1.9s vs 1.7s/页）。
- 两引擎在 DPI 72 与 150 下识别字数几乎一致（≈850–990 字），说明 **DPI 72 已足够覆盖古籍正文识别**，无需盲目拉高到 150。

### 4.2 引擎绝对耗时对比
- RapidOCR 单页耗时约为 PaddleOCR 的 **1/13**（72 DPI：1.7s vs 21.9s；150 DPI：1.9s vs 28.0s）。
- 同 5 页总耗时：RapidOCR 8.8–10.1s vs PaddleOCR 112.7–152.5s。
- 识别字数两者接近（差异 < 15%），RapidOCR 在速度上具有压倒性优势。

### 4.3 进程单例稳定性
- PaddleOCR 单实例连续推理 10 页，单页均值 27.4s 且波动小，验证进程级单例 + CPU 推理优化有效，**无逐页内存/耗时泄漏**。

## 5. 选型建议

| 场景 | 推荐引擎 | 理由 |
|------|---------|------|
| 批量整本古籍离线识别 | **RapidOCR** | 单页 1.7–1.9s，吞吐约为 PaddleOCR 的 13×，整本百页级任务耗时可从小时级降至分钟级 |
| 需要 PP-OCRv6 高精度 / 字符级 bbox | **PaddleOCR** | 模型更新、精度更高；进程单例下连续推理稳定，适合对精度敏感的小批量任务 |
| 默认 DPI | **72** | 与 150 识别字数基本一致，但耗时低 12–28% |

## 6. 已知观察与遗留项

### 6.1 字符级 bbox 开销（已实测，2026-07-18）
- 早期基准（DB 分层 Phase 1 之前）跑在 char_boxes 落地之前，`run_page` 返回的**行级** `boxes` 长度为 0，故未计入字符级 bbox 开销。
- 现状：`PaddleOCRAdapter` 已启用 `return_word_box=True`（`adapters.py:60`），`char_boxes` 字段正常返回（真实引擎单页 801 个逐字框，见 DB 分层验证）。RapidOCR 无字符级框，`char_boxes=None`。
- **char_boxes 耗时对照基准**（`scripts/bench_char_boxes.py`，DPI=72，5 页，同页先后跑 OFF `return_word_box=False` / ON `return_word_box=True`，含解析）：

  | 模式 | 单页均值(s) | 5 页逐字框总量 | 行数 |
  |------|------------:|---------------:|-----:|
  | OFF（关逐字框） | 21.1 | 0 | 0 |
  | ON（开逐字框）  | 21.0 | 3521 | 171 |
  | **增量** | **−0.1s（−0.3%）** | — | — |

- **结论**：开启 `return_word_box=True` 的额外开销落在测量噪声内（< 1%，绝对增量 ≈ 0），可视为**零成本**。逐字框由识别头顺带输出，不触发额外的检测/识别前向，因此对端到端吞吐无实质影响。DPI 72 与 150 下开销同构（均为识别头增量），故仅以 DPI 72 代表。
- **遗留项（已消解）**：原「char_boxes 开启后二次耗时」待跟进 → 已实测，结论为零成本，不再阻塞。

> ⚠️ 实现注记（非性能）：PaddleOCR 3.7.0 已将 `.ocr()` 标记为弃用（DeprecationWarning，建议改用 `predict`），当前仍可正常返回（含 `return_word_box`）。适配器迁移到 `predict` 属独立 follow-up，不影响本结论。

### 6.2 测试环境固定
- 硬件随时间可能变化（本机 CPU 型号、负载），跨机器数值不可直接比较；本文数值仅表征**相对关系**（DPI 倍数、引擎倍速、单例稳定性），结论不依赖绝对秒数。
