# KZOCR v0.5 AMEND rc2 领域评审

- **评审角色**：领域专家
- **评审日期**：2026-07-10
- **评审版本**：v0.5-rc2

---

## 结论：通过（Approved）

### D4 `expected_depth` 参数化 — 已修正

Round 6 提出的关键问题（expected_depth 硬编码 2 段导致多卷本 100% 假阳性）已在 rc2 中修正：

- `check_hierarchy_anomaly()` 签名增加 `expected_depth: int = 2` 参数
- 支持 CLI `--expected-depth` 或 TOC 分析推断
- 默认值 2 对单卷本（如《秘方求真》）保持向后兼容

**裁决：** 设计可接受。具体范围的实施细节（depth 应在 TOC 解析阶段后检查，而非扫描时）可在 D4 实现阶段补充注释。

### 其余 D0–D3 领域评估

- **D0**（Config 扩展）：无领域影响，通过
- **D1**（异常分类）：无领域影响，通过
- **D2**（VLM 循环重试）：无领域影响，通过
- **D3**（断点续跑）：缓存含 PDF 原文透明，在 TCM 数据治理上可接受

### 综合结论

**Approved.** 无剩余领域关注点。四个 D 项在 TCM OCR 领域均合理，未遗漏领域特定边缘场景。
